import { DeleteIconDirective } from './delete-icon.directive';

describe('DeleteIconDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteIconDirective();
    expect(directive).toBeTruthy();
  });
});
